# Data Folder

Simpan file dataset di folder ini dengan nama:

```text
adult_dataset.csv
```

File ini berasal dari project lama Income Prediction.

Dataset tidak disertakan otomatis di draft ini. Copy manual dari folder project lama sebelum menjalankan:

```powershell
python train_model.py
```
